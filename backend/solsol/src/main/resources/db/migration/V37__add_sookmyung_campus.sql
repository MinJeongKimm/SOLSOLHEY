-- =====================================================
-- 숙명여자대학교 캠퍼스 추가
-- =====================================================

-- 숙명여자대학교 캠퍼스 추가 (중복 방지)
INSERT INTO campus (name)
SELECT '숙명여자대학교' WHERE NOT EXISTS (SELECT 1 FROM campus WHERE name = '숙명여자대학교');
